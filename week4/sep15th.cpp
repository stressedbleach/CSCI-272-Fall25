#include <iostream>
#include <string>

using namespace std;

int main(){
  
  
  /*
    string firstName;
    string lastName;

    
    
    cout << "Enter first name: " << endl;
    cin >> firstName;
    cout << "Enter last name: " << endl;
    cin >> lastName;

    string fullName = firstName + " " + lastName;
    
    cout << "Full name: " << fullName << endl;

    fullName += " Jr.";
    cout << "Full name (with suffix): " << fullName << endl;

    fullName = "Dr. " + fullName;
    // another way to do it: 
//  fullName.insert(0, "Dr. ");

    cout << "Full name with prefix added: " << fullName << endl;




    string message = "Hello, World!"; 

    int messageLength = message.length();
    int messageSize = message.size();

    cout << "length: " << messageLength << endl << "size: " << messageSize << endl;

    char firstChar = message[0];
    char lastChar = message[message.length()-1];

    cout << "first char = " << firstChar << endl << "last char = " << lastChar << endl;


    string str1 = "apple";
    string str2 = "banana";

    if (str1 == str2){
        cout << "both strings are equal" << endl;
    } else if (str1 > str2) {
        cout << "str1 is lexicographically greater than str2" << endl;
    } else {
        cout << "str1 is lexicographically less than str2" << endl;
    }
        
    */

    string str1 = "hello";
    string str2 = "Hello!" ;

    bool caseInsentivecompare(const string& str1, const string& str2){
        
    }
    
    return 0;

}